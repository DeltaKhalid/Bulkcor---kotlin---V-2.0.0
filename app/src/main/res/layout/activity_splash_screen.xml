<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/splash_background">

    <View
        android:id="@+id/outerRing"
        android:layout_width="184dp"
        android:layout_height="184dp"
        android:background="@drawable/bg_splash_ring_outer"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/innerRing"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:background="@drawable/bg_splash_ring_inner"
        app:layout_constraintBottom_toBottomOf="@id/outerRing"
        app:layout_constraintEnd_toEndOf="@id/outerRing"
        app:layout_constraintStart_toStartOf="@id/outerRing"
        app:layout_constraintTop_toTopOf="@id/outerRing" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/logoCard"
        android:layout_width="84dp"
        android:layout_height="84dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="42dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="@id/outerRing"
        app:layout_constraintEnd_toEndOf="@id/outerRing"
        app:layout_constraintStart_toStartOf="@id/outerRing"
        app:layout_constraintTop_toTopOf="@id/outerRing">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="18dp"
            android:scaleType="fitCenter"
            android:src="@mipmap/ic_launcher"
            android:contentDescription="@string/app_name" />

    </com.google.android.material.card.MaterialCardView>

    <ImageView
        android:id="@+id/loadingIndicator"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/dotted_loader_static"
        android:contentDescription="@null"
        app:layout_constraintBottom_toTopOf="@id/versionText"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="14dp" />

    <TextView
        android:id="@+id/versionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:textColor="@color/splash_version"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
